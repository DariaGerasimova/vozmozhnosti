
Функция ПередачаЕдget(Запрос)
	Ответ = Новый HTTPСервисОтвет(200);
	//{{КОНСТРУКТОР_ЗАПРОСА_С_ОБРАБОТКОЙ_РЕЗУЛЬТАТА
	// Данный фрагмент построен конструктором.
	// При повторном использовании конструктора, внесенные вручную изменения будут утеряны!!!
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	ЕдиницыИзмерения.Код КАК Код,
	|	ЕдиницыИзмерения.Наименование КАК Наименование
	|ИЗ
	|	Справочник.ЕдиницыИзмерения КАК ЕдиницыИзмерения";
	
	РезультатЗапроса = Запрос.Выполнить();
	
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	КодЭлемента = 0;
	Массив = новый массив;
	Пока ВыборкаДетальныеЗаписи.Следующий() Цикл
		если КодЭлемента <> ВыборкаДетальныеЗаписи.Код тогда
			КодЭлемента = ВыборкаДетальныеЗаписи.Код;
			Стр = новый Структура;
			Стр.Вставить("Наименование",ВыборкаДетальныеЗаписи.Наименование);
			Стр.Вставить("Код",ВыборкаДетальныеЗаписи.Код);
			Массив.Добавить(Стр);
		КонецЕсли;  
		
	КонецЦикла;
	Запись = новый ЗаписьJSON;
	Запись.УстановитьСтроку();
	ЗаписатьJSON(Запись,Массив);
	
	Ответ.УстановитьТелоИзСтроки(Запись.Закрыть());
	Ответ.Заголовки.Вставить("content-type","application/json");
	Возврат Ответ;
КонецФункции

Функция ПередачаБригадget(Запрос)
	Ответ = Новый HTTPСервисОтвет(200);
	//{{КОНСТРУКТОР_ЗАПРОСА_С_ОБРАБОТКОЙ_РЕЗУЛЬТАТА
	// Данный фрагмент построен конструктором.
	// При повторном использовании конструктора, внесенные вручную изменения будут утеряны!!!
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	Бригады.Код КАК Код,
	|	Бригады.Наименование КАК Наименование,
	|	БригадыСоставБригады.Сотрудник.Представление КАК СотрудникПредставление
	|ИЗ
	|	Справочник.Бригады.СоставБригады КАК БригадыСоставБригады
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Бригады КАК Бригады
	|		ПО БригадыСоставБригады.Ссылка = Бригады.Ссылка";
	
	РезультатЗапроса = Запрос.Выполнить();
	
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	КодЭлемента = 0;
	Массив = новый Массив;
	МассивТЧ = Неопределено;
	
	Пока ВыборкаДетальныеЗаписи.Следующий() Цикл
		если КодЭлемента <> ВыборкаДетальныеЗаписи.Код тогда 
			
			КодЭлемента = ВыборкаДетальныеЗаписи.Код;
			Стр = новый Структура;
			Если МассивТЧ <> Неопределено тогда
				Стр.Вставить("ТабЧасть",МассивТЧ);
				Массив.Добавить(Стр);
			КонецЕсли;
			
			Стр.Вставить("Код",ВыборкаДетальныеЗаписи.Код);
			Стр.Вставить("Наименование",ВыборкаДетальныеЗаписи.Наименование);
			МассивТЧ = новый Массив;
			СтрТЧ = новый Структура;
			СтрТЧ.Вставить("Сотрудник",ВыборкаДетальныеЗаписи.СотрудникПредставление);
			МассивТЧ.Добавить(СтрТЧ);
			Стр.Вставить("ТабЧасть",МассивТЧ);
			Массив.Добавить(Стр);
		КонецЕсли;
		
	КонецЦикла;
	
	
	Запись = новый ЗаписьJSON;
	Запись.УстановитьСтроку();
	ЗаписатьJSON(Запись,Массив);
	
	Ответ.УстановитьТелоИзСтроки(Запись.Закрыть());
	ответ.Заголовки.Вставить("content-type","application/json");
	Возврат Ответ;
КонецФункции

Функция ПередачаНоменклатурыget(Запрос)
	Ответ = Новый HTTPСервисОтвет(200);
	//{{КОНСТРУКТОР_ЗАПРОСА_С_ОБРАБОТКОЙ_РЕЗУЛЬТАТА
	// Данный фрагмент построен конструктором.
	// При повторном использовании конструктора, внесенные вручную изменения будут утеряны!!!
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	Номенклатура.Код КАК Код,
	|	Номенклатура.Наименование КАК Наименование,
	|	Номенклатура.Артикул КАК Артикул,
	|	Номенклатура.ЕдиницаИзмерения.Представление КАК ЕдиницаИзмерения,
	|	ПРЕДСТАВЛЕНИЕ(Номенклатура.Тип) КАК Тип,
	|	Номенклатура.Спецификация.Представление КАК Спецификация
	|ИЗ
	|	Справочник.Номенклатура КАК Номенклатура";
	
	РезультатЗапроса = Запрос.Выполнить();
	
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	КодЭлемента = 0;
	Массив = новый Массив;
	Пока ВыборкаДетальныеЗаписи.Следующий() Цикл
		Если ВыборкаДетальныеЗаписи.Код <> КодЭлемента тогда
			КодЭлемента = ВыборкаДетальныеЗаписи.Код;
			Стр = новый структура;
			Стр.Вставить("Код",ВыборкаДетальныеЗаписи.Код);
			Стр.Вставить("Наименование",ВыборкаДетальныеЗаписи.Наименование);
			Стр.Вставить("Артикул",ВыборкаДетальныеЗаписи.Артикул);
			Стр.Вставить("ЕдиницаИзмерения",ВыборкаДетальныеЗаписи.ЕдиницаИзмерения);
			Стр.Вставить("Тип",ВыборкаДетальныеЗаписи.Тип);
			Стр.Вставить("Спецификация",ВыборкаДетальныеЗаписи.Спецификация);
			массив.Добавить(Стр);
		КонецЕсли;	
	КонецЦикла;
	
	Запись = новый ЗаписьJSON;
	Запись.УстановитьСтроку();
	ЗаписатьJSON(Запись,Массив);
	
	Ответ.УстановитьТелоИзСтроки(Запись.Закрыть());
	Ответ.Заголовки.Вставить("content-type","application/json");
	
	Возврат Ответ;
КонецФункции

Функция ПередачаСкладовget(Запрос)
	Ответ = Новый HTTPСервисОтвет(200);
	//{{КОНСТРУКТОР_ЗАПРОСА_С_ОБРАБОТКОЙ_РЕЗУЛЬТАТА
	// Данный фрагмент построен конструктором.
	// При повторном использовании конструктора, внесенные вручную изменения будут утеряны!!!
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	Склады.Код КАК Код,
	|	Склады.Наименование КАК Наименование
	|ИЗ
	|	Справочник.Склады КАК Склады";
	
	РезультатЗапроса = Запрос.Выполнить();
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	КодЭлемента = 0;
	Массив = новый массив;
	Пока ВыборкаДетальныеЗаписи.Следующий() Цикл
		если КодЭлемента <> ВыборкаДетальныеЗаписи.Код тогда
			КодЭлемента = ВыборкаДетальныеЗаписи.Код;
			Стр = новый Структура;
			Стр.Вставить("Наименование",ВыборкаДетальныеЗаписи.Наименование);
			Стр.Вставить("Код",ВыборкаДетальныеЗаписи.Код);
			Массив.Добавить(Стр);
		КонецЕсли;  
		
	КонецЦикла;
	Запись = новый ЗаписьJSON;
	Запись.УстановитьСтроку();
	ЗаписатьJSON(Запись,Массив);
	
	Ответ.УстановитьТелоИзСтроки(Запись.Закрыть());
	Ответ.Заголовки.Вставить("content-type","application/json");
	Возврат Ответ;
КонецФункции

Функция ВидыРаботGet(Запрос)
	Ответ = Новый HTTPСервисОтвет(200);
	//{{КОНСТРУКТОР_ЗАПРОСА_С_ОБРАБОТКОЙ_РЕЗУЛЬТАТА
	// Данный фрагмент построен конструктором.
	// При повторном использовании конструктора, внесенные вручную изменения будут утеряны!!!
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	ВидыРабот.Код КАК Код,
	|	ВидыРабот.Наименование КАК Наименование
	|ИЗ
	|	Справочник.ВидыРабот КАК ВидыРабот";
	
	РезультатЗапроса = Запрос.Выполнить();
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	КодЭлемента = 0;
	Массив = новый массив;
	Пока ВыборкаДетальныеЗаписи.Следующий() Цикл
		если КодЭлемента <> ВыборкаДетальныеЗаписи.Код тогда
			КодЭлемента = ВыборкаДетальныеЗаписи.Код;
			Стр = новый Структура;
			Стр.Вставить("Наименование",ВыборкаДетальныеЗаписи.Наименование);
			Стр.Вставить("Код",ВыборкаДетальныеЗаписи.Код);
			Массив.Добавить(Стр);
		КонецЕсли;  
		
	КонецЦикла;
	Запись = новый ЗаписьJSON;
	Запись.УстановитьСтроку();
	ЗаписатьJSON(Запись,Массив);
	
	Ответ.УстановитьТелоИзСтроки(Запись.Закрыть());
	Ответ.Заголовки.Вставить("content-type","application/json");
	Возврат Ответ;
	
КонецФункции

Функция ПередачаСпецификацийget(Запрос)
	Ответ = Новый HTTPСервисОтвет(200);
	//{{КОНСТРУКТОР_ЗАПРОСА_С_ОБРАБОТКОЙ_РЕЗУЛЬТАТА
	// Данный фрагмент построен конструктором.
	// При повторном использовании конструктора, внесенные вручную изменения будут утеряны!!!
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	Спецификации.Код КАК Код,
	|	Спецификации.Наименование КАК Наименование,
	|	Спецификации.ВыходноеИзделие.Представление КАК ВыходноеИзделиеПредставление,
	|	Спецификации.Количество КАК Количество,
	|	Спецификации.ЕдиницаИзмерения.Представление КАК ЕдиницаИзмеренияПредставление,
	|	ПРЕДСТАВЛЕНИЕ(Спецификации.Статус) КАК СтатусПредставление,
	|	СпецификацииПереченьМатериалов.Номенклатура.Представление КАК НоменклатураПредставление,
	|	СпецификацииПереченьМатериалов.Количество КАК Количество1,
	|	СпецификацииЭтапыРаботы.ВидРаботы.Представление КАК ВидРаботыПредставление,
	|	СпецификацииЭтапыРаботы.Время КАК Время
	|ИЗ
	|	Справочник.Спецификации.ПереченьМатериалов КАК СпецификацииПереченьМатериалов
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Спецификации КАК Спецификации
	|			ПРАВОЕ СОЕДИНЕНИЕ Справочник.Спецификации.ЭтапыРаботы КАК СпецификацииЭтапыРаботы
	|			ПО СпецификацииЭтапыРаботы.Ссылка = Спецификации.Ссылка
	|		ПО СпецификацииПереченьМатериалов.Ссылка = Спецификации.Ссылка";
	
	РезультатЗапроса = Запрос.Выполнить();
	
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	КодЭлемента = 0;
	Массив = новый массив;
	МассивТЧ1 = Неопределено;
	МассивТЧ2 = Неопределено;
	Пока ВыборкаДетальныеЗаписи.Следующий() Цикл
		если КодЭлемента <> ВыборкаДетальныеЗаписи.Код тогда 
			стр = новый Структура;
			если МассивТЧ1 <> Неопределено тогда
				Стр.Вставить("ТабЧасть1",МассивТЧ1);
				массив.Добавить(Стр);
			КонецЕсли;
			
			если МассивТЧ2 <> Неопределено тогда
				Стр.Вставить("ТабЧасть2",МассивТЧ2);
				Массив.Добавить(Стр);
			КонецЕсли;
			
			КодЭлемента = ВыборкаДетальныеЗаписи.Код;
			Стр.Вставить("Код",ВыборкаДетальныеЗаписи.Код);
			Стр.Вставить("Наименование",ВыборкаДетальныеЗаписи.Наименование);
			Стр.Вставить("ВыходноеИзделие",ВыборкаДетальныеЗаписи.ВыходноеИзделиеПредставление);
			Стр.Вставить("Количество",ВыборкаДетальныеЗаписи.Количество);
			Стр.Вставить("ЕдиницаИзмерения",ВыборкаДетальныеЗаписи.ЕдиницаИзмеренияПредставление);
			Стр.Вставить("Статус",ВыборкаДетальныеЗаписи.СтатусПредставление);
			
			МассивТЧ1 = новый Массив;
			СтрТЧ1 = новый Структура;
			СтрТЧ1.Вставить("Номенклатура",ВыборкаДетальныеЗаписи.НоменклатураПредставление);
			СтрТЧ1.Вставить("КоличествоН",ВыборкаДетальныеЗаписи.Количество1);
			МассивТЧ1.Добавить(СтрТЧ1);			
			МассивТЧ2 = новый Массив;
			СтрТЧ2 = новый Структура;
			СтрТЧ2.Вставить("ВидРаботы",ВыборкаДетальныеЗаписи.ВидРаботыПредставление);
			СтрТЧ2.Вставить("Время",ВыборкаДетальныеЗаписи.Время);
			МассивТЧ2.Добавить(СтрТЧ2);
			
			Стр.Вставить("ТабЧасть1",МассивТЧ1);
			Стр.Вставить("ТабЧасть2",МассивТЧ2);
			Массив.Добавить(Стр);
			
		КонецЕсли;
		
	КонецЦикла;
	
	Запись = новый ЗаписьJSON;
	Запись.УстановитьСтроку();
	ЗаписатьJSON(Запись,Массив);
	
	Ответ.УстановитьТелоИзСтроки(Запись.Закрыть());
	Ответ.Заголовки.Вставить("content-type","application/json");
	
	Возврат Ответ;
КонецФункции

Функция ПередачаСотрудниковget(Запрос)
	Ответ = Новый HTTPСервисОтвет(200);
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	Сотрудники.Код КАК Код,
	|	Сотрудники.Наименование КАК Наименование
	|ИЗ
	|	Справочник.Сотрудники КАК Сотрудники";
	
	РезультатЗапроса = Запрос.Выполнить();
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	КодЭлемента = 0;
	Массив = новый массив;
	Пока ВыборкаДетальныеЗаписи.Следующий() Цикл
		если КодЭлемента <> ВыборкаДетальныеЗаписи.Код тогда
			КодЭлемента = ВыборкаДетальныеЗаписи.Код;
			Стр = новый Структура;
			Стр.Вставить("Наименование",ВыборкаДетальныеЗаписи.Наименование);
			Стр.Вставить("Код",ВыборкаДетальныеЗаписи.Код);
			Массив.Добавить(Стр);
		КонецЕсли;  
		
	КонецЦикла;
	Запись = новый ЗаписьJSON;
	Запись.УстановитьСтроку();
	ЗаписатьJSON(Запись,Массив);
	
	Ответ.УстановитьТелоИзСтроки(Запись.Закрыть());
	Ответ.Заголовки.Вставить("content-type","application/json");
	Возврат Ответ;
	
КонецФункции

Функция ПередачаЗаказовget(Запрос)
	Ответ = Новый HTTPСервисОтвет(200);
	//{{КОНСТРУКТОР_ЗАПРОСА_С_ОБРАБОТКОЙ_РЕЗУЛЬТАТА
	// Данный фрагмент построен конструктором.
	// При повторном использовании конструктора, внесенные вручную изменения будут утеряны!!!
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	ЗаказыНаПроизводство.Дата КАК Дата,
	|	ЗаказыНаПроизводство.Номер КАК Номер,
	|	ЗаказыНаПроизводство.Проведен КАК Проведен,
	|	ЗаказыНаПроизводствоСписокИзделий.Изделие.Представление КАК ИзделиеПредставление,
	|	ЗаказыНаПроизводствоСписокИзделий.Количество КАК Количество,
	|	ЗаказыНаПроизводствоСписокИзделий.ЕдиницаИзмерения.Представление КАК ЕдиницаИзмеренияПредставление,
	|	ЗаказыНаПроизводствоСписокИзделий.Спецификация.Представление КАК СпецификацияПредставление
	|ИЗ
	|	Документ.ЗаказыНаПроизводство.СписокИзделий КАК ЗаказыНаПроизводствоСписокИзделий
	|		ЛЕВОЕ СОЕДИНЕНИЕ Документ.ЗаказыНаПроизводство КАК ЗаказыНаПроизводство
	|		ПО ЗаказыНаПроизводствоСписокИзделий.Ссылка = ЗаказыНаПроизводство.Ссылка";
	
	РезультатЗапроса = Запрос.Выполнить();
	
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	НомерДокумента = 0;
	Массив = новый массив;
	МассивТЧ = Неопределено;
	
	Пока ВыборкаДетальныеЗаписи.Следующий() Цикл
		Если НомерДокумента<>ВыборкаДетальныеЗаписи.Номер тогда
			НомерДокумента = ВыборкаДетальныеЗаписи.Номер;
			Стр = новый Структура;
			если МассивТЧ <> Неопределено тогда
				Стр.Вставить("ТабЧасть",МассивТЧ);
				Массив.Добавить(Стр);
			КонецЕсли;
			Стр.Вставить("Дата",ВыборкаДетальныеЗаписи.Дата);
			Стр.Вставить("Номер",ВыборкаДетальныеЗаписи.Номер);
			Стр.Вставить("Проведен",ВыборкаДетальныеЗаписи.Проведен);
			
			МассивТЧ = новый Массив;
			СтрТЧ = новый Структура;
			СтрТЧ.Вставить("Изделие",ВыборкаДетальныеЗаписи.ИзделиеПредставление);
			СтрТЧ.Вставить("Количество",ВыборкаДетальныеЗаписи.Количество);
			СтрТЧ.Вставить("ЕдИзм", ВыборкаДетальныеЗаписи.ЕдиницаИзмеренияПредставление);
			СтрТЧ.Вставить("Спецификация",ВыборкаДетальныеЗаписи.СпецификацияПредставление);
			МассивТЧ.Добавить(СтрТЧ);
			//
			Стр.Вставить("ТабЧасть",МассивТЧ);
			Массив.Добавить(Стр);
		КонецЕсли;	
	КонецЦикла; 
	Запись = новый ЗаписьJSON;
	Запись.УстановитьСтроку();
	ЗаписатьJSON(Запись,Массив);
	
	Ответ.УстановитьТелоИзСтроки(Запись.Закрыть());
	Ответ.Заголовки.Вставить("content-type","application/json");
	Возврат Ответ;
	
	
	Возврат Ответ;
КонецФункции

Функция ПередачаЭтаповget(Запрос)
	Ответ = Новый HTTPСервисОтвет(200); 
	//{{КОНСТРУКТОР_ЗАПРОСА_С_ОБРАБОТКОЙ_РЕЗУЛЬТАТА
	// Данный фрагмент построен конструктором.
	// При повторном использовании конструктора, внесенные вручную изменения будут утеряны!!!
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	ВыполнениеЭтапаПроизводства.Номер КАК Номер,
	|	ВыполнениеЭтапаПроизводства.Дата КАК Дата,
	|	ВыполнениеЭтапаПроизводства.Проведен КАК Проведен,
	|	ВыполнениеЭтапаПроизводства.Изделие.Представление КАК ИзделиеПредставление,
	|	ВыполнениеЭтапаПроизводства.Количество КАК Количество,
	|	ВыполнениеЭтапаПроизводства.ЕдиницаИзмерения.Представление КАК ЕдиницаИзмеренияПредставление,
	|	ВыполнениеЭтапаПроизводства.Бригада.Представление КАК БригадаПредставление,
	|	ВыполнениеЭтапаПроизводства.СкладГотовойПродукции.Представление КАК СкладГотовойПродукцииПредставление,
	|	ПРЕДСТАВЛЕНИЕ(ВыполнениеЭтапаПроизводства.Статус) КАК СтатусПредставление,
	|	ВыполнениеЭтапаПроизводства.СкладМатериалов.Представление КАК СкладМатериаловПредставление,
	|	ВыполнениеЭтапаПроизводства.СуммаЗатратПоДокументу КАК СуммаЗатратПоДокументу,
	|	ВыполнениеЭтапаПроизводства.СуммаТрудозатрат КАК СуммаТрудозатрат,
	|	ВыполнениеЭтапаПроизводства.СуммаМатериальныхЗатрат КАК СуммаМатериальныхЗатрат,
	|	ВыполнениеЭтапаПроизводстваВидыРабот.ВидРаботы.Представление КАК ВидРаботыПредставление,
	|	ВыполнениеЭтапаПроизводстваВидыРабот.Цена КАК Цена,
	|	ВыполнениеЭтапаПроизводстваВидыРабот.Время КАК Время,
	|	ВыполнениеЭтапаПроизводстваВидыРабот.Сумма КАК Сумма,
	|	ВыполнениеЭтапаПроизводстваЗатраченныеМатериалы.Номенклатура.Представление КАК НоменклатураПредставление,
	|	ВыполнениеЭтапаПроизводстваЗатраченныеМатериалы.Количество КАК Количество1,
	|	ВыполнениеЭтапаПроизводстваЗатраченныеМатериалы.Цена КАК Цена1,
	|	ВыполнениеЭтапаПроизводстваЗатраченныеМатериалы.Сумма КАК Сумма1,
	|	ВыполнениеЭтапаПроизводстваЗатраченныеМатериалы.ЕдиницаИзмерения.Представление КАК ЕдиницаИзмеренияПредставление1,
	|	ВыполнениеЭтапаПроизводства.ЗаказНаПроизводство.Номер КАК ЗаказНаПроизводствоНомер
	|ИЗ
	|	Документ.ВыполнениеЭтапаПроизводства КАК ВыполнениеЭтапаПроизводства
	|		ЛЕВОЕ СОЕДИНЕНИЕ Документ.ВыполнениеЭтапаПроизводства.ЗатраченныеМатериалы КАК ВыполнениеЭтапаПроизводстваЗатраченныеМатериалы
	|		ПО ВыполнениеЭтапаПроизводства.Ссылка = ВыполнениеЭтапаПроизводстваЗатраченныеМатериалы.Ссылка
	|		ЛЕВОЕ СОЕДИНЕНИЕ Документ.ВыполнениеЭтапаПроизводства.ВидыРабот КАК ВыполнениеЭтапаПроизводстваВидыРабот
	|		ПО (ВыполнениеЭтапаПроизводстваВидыРабот.Ссылка = ВыполнениеЭтапаПроизводства.Ссылка)";
	
	РезультатЗапроса = Запрос.Выполнить();
	
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	Массив = новый Массив;
	МассивТЧ1 = Неопределено;
	МассивТЧ2 = Неопределено;
	НомерДокумента = 0;
	Пока ВыборкаДетальныеЗаписи.Следующий() Цикл
		если ВыборкаДетальныеЗаписи.Номер <> НомерДокумента тогда
			НомерДокумента = ВыборкаДетальныеЗаписи.Номер;
			Стр = новый Структура;
			Если МассивТЧ1 <> Неопределено тогда 
				Стр.Вставить("ТабЧасть1",МассивТЧ1);
				Массив.Добавить(Стр);
			КонецЕсли;
			Если МассивТЧ2 <> Неопределено тогда 
				Стр.Вставить("ТабЧасть2",МассивТЧ2);
				Массив.Добавить(Стр);
			КонецЕсли;
			Стр.Вставить("Номер",ВыборкаДетальныеЗаписи.Номер);
			Стр.Вставить("Дата",ВыборкаДетальныеЗаписи.Дата);
			Стр.Вставить("Проведен",ВыборкаДетальныеЗаписи.Проведен);
			Стр.Вставить("Заказ",ВыборкаДетальныеЗаписи.ЗаказНаПроизводствоНомер);
			Стр.Вставить("Изделие",ВыборкаДетальныеЗаписи.ИзделиеПредставление);
			Стр.Вставить("Количество",ВыборкаДетальныеЗаписи.Количество);
			Стр.Вставить("ЕдИзм",ВыборкаДетальныеЗаписи.ЕдиницаИзмеренияПредставление);
			Стр.Вставить("Бригада",ВыборкаДетальныеЗаписи.БригадаПредставление);
			Стр.Вставить("СкладГ",ВыборкаДетальныеЗаписи.СкладГотовойПродукцииПредставление);
			Стр.Вставить("Статус",ВыборкаДетальныеЗаписи.СтатусПредставление);
			Стр.Вставить("СкладМ",ВыборкаДетальныеЗаписи.СкладМатериаловПредставление);
			Стр.Вставить("СуммаЗатратПоДокументу",ВыборкаДетальныеЗаписи.СуммаЗатратПоДокументу);
			Стр.Вставить("СуммаТрудозатрат",ВыборкаДетальныеЗаписи.СуммаТрудозатрат);
			Стр.Вставить("СуммаМатериальныхЗатрат",ВыборкаДетальныеЗаписи.СуммаМатериальныхЗатрат);
			
			МассивТЧ1 = новый Массив;
			СтрТЧ1 = новый Структура;
			СтрТЧ1.Вставить("ВидРаботы",ВыборкаДетальныеЗаписи.ВидРаботыПредставление);
			СтрТЧ1.Вставить("Цена",ВыборкаДетальныеЗаписи.Цена);
			СтрТЧ1.Вставить("Время",ВыборкаДетальныеЗаписи.Время);
			СтрТЧ1.Вставить("Сумма",ВыборкаДетальныеЗаписи.Сумма);
			МассивТЧ1.Добавить(СтрТЧ1);
			
			МассивТЧ2 = новый Массив;
			СтрТЧ2 = новый Структура;
			СтрТЧ2.Вставить("Номенклатура1",ВыборкаДетальныеЗаписи.НоменклатураПредставление);
			СтрТЧ2.Вставить("Цена1",ВыборкаДетальныеЗаписи.Цена1);
			СтрТЧ2.Вставить("Сумма1",ВыборкаДетальныеЗаписи.Сумма1);
			СтрТЧ2.Вставить("ЕдИзм1",ВыборкаДетальныеЗаписи.ЕдиницаИзмеренияПредставление1);
			СтрТЧ2.Вставить("КолВО",ВыборкаДетальныеЗаписи.Количество1);
			МассивТЧ2.Добавить(СтрТЧ2);
			
			Стр.Вставить("ТабЧасть1",МассивТЧ1);
			Стр.Вставить("ТабЧасть2",МассивТЧ2);
			Массив.Добавить(Стр);
			
			
		КонецЕсли;
		
	КонецЦикла;
	
	Запись = новый ЗаписьJSON;
	Запись.УстановитьСтроку();
	ЗаписатьJSON(Запись,Массив);
	
	Ответ.УстановитьТелоИзСтроки(Запись.Закрыть());
	Ответ.Заголовки.Вставить("content-type","application/json");
	Возврат Ответ;
	
	
	Возврат Ответ;
КонецФункции

Функция ПередачаПеремещенийget(Запрос)
	Ответ = Новый HTTPСервисОтвет(200);
	//{{КОНСТРУКТОР_ЗАПРОСА_С_ОБРАБОТКОЙ_РЕЗУЛЬТАТА
	// Данный фрагмент построен конструктором.
	// При повторном использовании конструктора, внесенные вручную изменения будут утеряны!!!
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	ПеремещениеМатериалов.Номер КАК Номер,
	|	ПеремещениеМатериалов.Дата КАК Дата,
	|	ПеремещениеМатериалов.Проведен КАК Проведен,
	|	ПеремещениеМатериалов.СкладОтправитель.Представление КАК СкладОтправительПредставление,
	|	ПеремещениеМатериалов.СкладПолучатель.Представление КАК СкладПолучательПредставление,
	|	ПеремещениеМатериаловТабличнаяЧасть1.Номенклатура.Представление КАК НоменклатураПредставление,
	|	ПеремещениеМатериаловТабличнаяЧасть1.Количество КАК Количество
	|ИЗ
	|	Документ.ПеремещениеМатериалов.ТабличнаяЧасть1 КАК ПеремещениеМатериаловТабличнаяЧасть1
	|		ЛЕВОЕ СОЕДИНЕНИЕ Документ.ПеремещениеМатериалов КАК ПеремещениеМатериалов
	|		ПО ПеремещениеМатериаловТабличнаяЧасть1.Ссылка = ПеремещениеМатериалов.Ссылка";
	
	РезультатЗапроса = Запрос.Выполнить();
	
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	Массив = новый Массив;
	МассивТЧ = Неопределено;
	НомерДокумента = 0;
	Пока ВыборкаДетальныеЗаписи.Следующий() Цикл
		Если НомерДокумента <> ВыборкаДетальныеЗаписи.Номер тогда
			Стр = новый Структура;
			
			Если МассивТЧ <> Неопределено тогда
				Стр.Вставить("ТабЧасть1",МассивТЧ);
				Массив.Добавить(Стр);
			КонецЕсли;        
			
			Стр.Вставить("Номер",ВыборкаДетальныеЗаписи.Номер);
			Стр.Вставить("Дата",ВыборкаДетальныеЗаписи.Дата);
			Стр.Вставить("Проведен",ВыборкаДетальныеЗаписи.Проведен);
			Стр.Вставить("СкладО",ВыборкаДетальныеЗаписи.СкладОтправительПредставление);
			Стр.Вставить("СкладП",ВыборкаДетальныеЗаписи.СкладПолучательПредставление); 
			
			МассивТЧ = новый Массив;
			СтрТЧ = Новый Структура;
			СтрТЧ.Вставить("НоменклатураПредставление",ВыборкаДетальныеЗаписи.НоменклатураПредставление);
			СтрТЧ.Вставить("Количество",ВыборкаДетальныеЗаписи.Количество);
			МассивТЧ.Добавить(СтрТЧ);
			
			Стр.Вставить("ТабЧасть1",МассивТЧ);
			Массив.Добавить(Стр);
			
		КонецЕсли;
		
	КонецЦикла;
	Запись = новый ЗаписьJSON;
	Запись.УстановитьСтроку();
	ЗаписатьJSON(Запись,Массив);
	
	Ответ.УстановитьТелоИзСтроки(Запись.Закрыть());
	ответ.Заголовки.Вставить("content-type","application/json");
	Возврат Ответ;
	
КонецФункции

Функция ПередачаРеализацииПродукцииget(Запрос)
	Ответ = Новый HTTPСервисОтвет(200); 
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	РеализацияГотовойПродукции.Номер КАК Номер,
		|	РеализацияГотовойПродукции.Дата КАК Дата,
		|	РеализацияГотовойПродукции.Проведен КАК Проведен,
		|	РеализацияГотовойПродукции.Склад.Представление КАК СкладПредставление,
		|	РеализацияГотовойПродукцииТабличнаяЧасть1.Номенклатура.Представление КАК НоменклатураПредставление,
		|	РеализацияГотовойПродукцииТабличнаяЧасть1.Количество КАК Количество,
		|	РеализацияГотовойПродукцииТабличнаяЧасть1.Цена КАК Цена,
		|	РеализацияГотовойПродукцииТабличнаяЧасть1.Сумма КАК Сумма
		|ИЗ
		|	Документ.РеализацияГотовойПродукции КАК РеализацияГотовойПродукции
		|		ЛЕВОЕ СОЕДИНЕНИЕ Документ.РеализацияГотовойПродукции.ТабличнаяЧасть1 КАК РеализацияГотовойПродукцииТабличнаяЧасть1
		|		ПО РеализацияГотовойПродукцииТабличнаяЧасть1.Ссылка = РеализацияГотовойПродукции.Ссылка";
	
	РезультатЗапроса = Запрос.Выполнить();
	
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	НомерДокумента = 0;
	Массив = новый массив;
	МассивТЧ = Неопределено;
	Пока ВыборкаДетальныеЗаписи.Следующий() Цикл
		если ВыборкаДетальныеЗаписи.Номер <> НомерДокумента тогда 
			НомерДокумента = ВыборкаДетальныеЗаписи.Номер;
			Стр = новый Структура;
			
			Если МассивТЧ <> Неопределено тогда
				Стр.Вставить("ТабЧасть",МассивТЧ);
				Массив.Добавить(Стр);
			КонецЕсли;
			Стр.Вставить("Номер",ВыборкаДетальныеЗаписи.Номер);
			Стр.Вставить("Дата",ВыборкаДетальныеЗаписи.Дата);
			Стр.Вставить("Проведен",ВыборкаДетальныеЗаписи.Проведен);
			Стр.Вставить("Склад",ВыборкаДетальныеЗаписи.СкладПредставление);
			
			МассивТЧ = новый массив;
			СтрТЧ = новый Структура;
			СтрТЧ.Вставить("НоменклатураТЧ",ВыборкаДетальныеЗаписи.НоменклатураПредставление);
			СтрТЧ.Вставить("Количество",ВыборкаДетальныеЗаписи.Количество);
			СтрТЧ.Вставить("Цена",ВыборкаДетальныеЗаписи.Цена);
			СтрТЧ.Вставить("Сумма",ВыборкаДетальныеЗаписи.Сумма);
			МассивТЧ.Добавить(СтрТЧ);
			Стр.Вставить("ТабЧасть",МассивТЧ);
			
			Массив.Добавить(Стр);
		КонецЕсли;
		
	КонецЦикла;
	

	
	Возврат Ответ;
КонецФункции

Функция ПередачаЗакупокget(Запрос)
	Ответ = Новый HTTPСервисОтвет(200);
	Возврат Ответ;
КонецФункции

Функция ПриемПеремещенийpost(Запрос)
	Ответ = Новый HTTPСервисОтвет(200); 
	СтрокаПеремещений = Запрос.ПолучитьТелоКакСтроку();
	ДанныеСМобильногоПриложения.ЗагрузитьПеремещение(СтрокаПеремещений);
	Возврат Ответ;
КонецФункции

Функция ПриемЭтаповpost(Запрос)
	Ответ = Новый HTTPСервисОтвет(200);
	СтрокаЭтапов = Запрос.ПолучитьТелоКакСтроку();
	ДанныеСМобильногоПриложения.ЗагрузитьВыполненияЭтапов(СтрокаЭтапов);
	Возврат Ответ;
КонецФункции

Функция ПриемБраковPOST(Запрос)
	Ответ = Новый HTTPСервисОтвет(200);  
	СтрокаБраков = Запрос.ПолучитьТелоКакСтроку();
	ДанныеСМобильногоПриложения.ЗагрузитьБрак(СтрокаБраков);
	Возврат Ответ;
КонецФункции


Функция ЗапросОстатковМетод1(Запрос)
	Ответ = Новый HTTPСервисОтвет(200);
	Склад = Запрос.ПараметрыURL.Получить("Склад");
	Если Склад = Неопределено тогда
		СписокОстатков = СписокОстатков();
		Ответ.УстановитьТелоИзСтроки(СписокОстатков);
	    ответ.Заголовки.Вставить("Content-type","application/json");
	Возврат Ответ;     
Иначе
	СписокОстатков = СписокОстатковПоСкладу(Склад);
	Ответ.УстановитьТелоИзСтроки(СписокОстатков);
	    ответ.Заголовки.Вставить("Content-type","application/json");
		возврат Ответ;
	КонецЕсли;
	
КонецФункции

Функция СписокОстатков()

	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	УчетНоменклатурыОстаткиИОбороты.КоличествоКонечныйОстаток КАК КоличествоКонечныйОстаток,
		|	УчетНоменклатурыОстаткиИОбороты.Склад.Наименование КАК СкладНаименование,
		|	УчетНоменклатурыОстаткиИОбороты.Номенклатура.Наименование КАК НоменклатураНаименование
		|ИЗ
		|	РегистрНакопления.УчетНоменклатуры.ОстаткиИОбороты КАК УчетНоменклатурыОстаткиИОбороты
		|ИТОГИ ПО
		|	СкладНаименование";
	
	РезультатЗапроса = Запрос.Выполнить();
	
	ВыборкаСклад = РезультатЗапроса.Выбрать(ОбходРезультатаЗапроса.ПоГруппировкам);
	Массив = новый Массив;
	МассивТЧ = Неопределено;
	Пока ВыборкаСклад.Следующий() Цикл
	    Стр = новый Структура;
		Стр.Вставить("Склад",ВыборкаСклад.СкладНаименование);
		МассивТЧ = новый Массив;
		ВыборкаДетальныеЗаписи = ВыборкаСклад.Выбрать();
	
		Пока ВыборкаДетальныеЗаписи.Следующий() Цикл
			СтрТЧ = новый Структура;
			СтрТЧ.Вставить("Номенклатура",ВыборкаДетальныеЗаписи.НоменклатураНаименование);
			СтрТЧ.Вставить("КоличествоКонечныйОстаток",ВыборкаДетальныеЗаписи.КоличествоКонечныйОстаток);
			МассивТЧ.Добавить(СтрТЧ);
		КонецЦикла;
		Стр.Вставить("СписокНоменклатуры",МассивТЧ);
		Массив.Добавить(Стр);
	КонецЦикла;
	 запись = новый ЗаписьJSON;
	 запись.УстановитьСтроку();
	 ЗаписатьJSON(Запись,Массив);
	 
	 Джсон = запись.Закрыть();

     возврат Джсон;
КонецФункции // СписокОстатков()

функция СписокОстатковПоСкладу(Склад)

	//{{КОНСТРУКТОР_ЗАПРОСА_С_ОБРАБОТКОЙ_РЕЗУЛЬТАТА
	// Данный фрагмент построен конструктором.
	// При повторном использовании конструктора, внесенные вручную изменения будут утеряны!!!
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	УчетНоменклатурыОстаткиИОбороты.Номенклатура.Наименование КАК НоменклатураНаименование,
		|	УчетНоменклатурыОстаткиИОбороты.КоличествоКонечныйОстаток КАК КоличествоКонечныйОстаток
		|ИЗ
		|	РегистрНакопления.УчетНоменклатуры.ОстаткиИОбороты КАК УчетНоменклатурыОстаткиИОбороты
		|ГДЕ
		|	УчетНоменклатурыОстаткиИОбороты.Склад = &Склад";
	
	Запрос.УстановитьПараметр("Склад", Склад);
	
	РезультатЗапроса = Запрос.Выполнить();
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	Массив = новый Массив;
	Пока ВыборкаДетальныеЗаписи.Следующий() Цикл
        Стр = новый Структура;
		Стр.Вставить("Номеклатура",ВыборкаДетальныеЗаписи.НоменклатураНаименование);
		Стр.Вставить("КоличествоОстаток",ВыборкаДетальныеЗаписи.КоличествоКонечныйОстаток);
		Массив.Добавить(Стр);
	КонецЦикла;
	 запись = новый ЗаписьJSON;
	 запись.УстановитьСтроку();
	 ЗаписатьJSON(Запись,Массив);
	 
	 Джсон = запись.Закрыть();

     возврат Джсон;
КонецФункции
//////////////////////////////////////////////////////////////////////////////////////////////////////////////////
Процедура СоздатьПеремещение(Стр) 
 Проверка = документы.ПеремещениеМатериалов.НайтиПоНомеру(Стр.Номер);
	
	если проверка.Пустая() тогда
		Спр = документы.ПеремещениеМатериалов.СоздатьДокумент();
	иначе
		спр = проверка.ПолучитьОбъект();
	КонецЕсли;
	
	Спр.Номер = Стр.Номер;
	Спр.Дата = ПрочитатьДатуJSON(Стр.Дата,ФорматДатыJSON.ISO);
	Спр.Проведен = Стр.Проведен;
	Спр.СкладОтправитель = справочники.Склады.НайтиПоНаименованию(Стр.СкладО);
	Спр.СкладПолучатель = справочники.Склады.НайтиПоНаименованию(Стр.СкладП);
	
	Для каждого СтрТЧ из Стр.ТабЧасть1 цикл
		СпрТЧ = спр.ТабличнаяЧасть1.Добавить();
		СпрТЧ.Номенклатура = Справочники.Номенклатура.НайтиПоНаименованию(СтрТЧ.НоменклатураПредставление);
		СпрТЧ.Количество = СтрТЧ.Количество;
		
	КонецЦикла;
	    спр.Записать(РежимЗаписиДокумента.Проведение);
	

	КонецПроцедуры 
	
	Процедура ЗагрузитьПеремещение(СтрокаПеремещения) экспорт
	
		Чтение = новый ЧтениеJSON();
		Чтение.УстановитьСтроку(СтрокаПеремещения);
		ПеремещениеМассив = ПрочитатьJSON(Чтение);
		Для каждого СтрокаМассива из ПеремещениеМассив цикл
			СоздатьПеремещение(СтрокаМассива);
		КонецЦикла;
		
	
	КонецПроцедуры 
	
	процедура ЗагрузитьВыполненияЭтапов(СтрокаВыполнения) экспорт
	чтение = новый ЧтениеJSON;
	Чтение.УстановитьСтроку(СтрокаВыполнения);
	ЭтапыМассив = ПрочитатьJSON(Чтение);
	Для каждого СтрокаМассива из ЭтапыМассив цикл
		СоздатьВыполнениеЭтапа(СтрокаМассива);
	КонецЦикла;
	
	
	КонецПроцедуры
	
	
	процедура СоздатьВыполнениеЭтапа(Стр)
	Проверка = документы.ВыполнениеЭтапаПроизводства.НайтиПоНомеру(Стр.Номер);
	
	Если проверка.Пустая() тогда
		спр = документы.ВыполнениеЭтапаПроизводства.СоздатьДокумент();
	иначе
		спр = проверка.ПолучитьОбъект();
	КонецЕсли;
	
	   Спр.Номер = Стр.Номер;
	   Спр.Дата = ПрочитатьДатуJSON(Стр.Дата,ФорматДатыJSON);
	   Спр.Бригада = справочники.Бригады.НайтиПоНаименованию(Стр.Бригада);
	   Спр.Проведен = Стр.Проведен;
	   Спр.ЗаказНаПроизводство = Документы.ЗаказыНаПроизводство.НайтиПоНомеру(Стр.Заказ);
	   Спр.Изделие = Справочники.Номенклатура.НайтиПоНаименованию(Стр.Изделие);
	   Спр.Количество = Стр.Количество;
	   Спр.ЕдиницаИзмерения = Справочники.ЕдиницыИзмерения.НайтиПоНаименованию(Стр.ЕдИзм); 
	   Если Стр.Статус = "В работе" тогда
	   Спр.Статус = перечисления.СтатусыЭтаповПроизводства.ВРаботе;
   ИначеЕсли Стр.Статус = "Завершен" тогда
	   Спр.статус = перечисления.СтатусыЭтаповПроизводства.Завершен;
   ИначеЕсли Стр.Статус = "Запланирован" тогда
	   Спр.Статус = перечисления.СтатусыЭтаповПроизводства.Запланирован
   КонецЕсли; 
   Спр.СкладМатериалов = справочники.Склады.НайтиПоНаименованию(Стр.СкладМ);
   Спр.СкладГотовойПродукции = справочники.Склады.НайтиПоНаименованию(Стр.СкладГ);
   Спр.СуммаЗатратПоДокументу = Стр.СуммаЗатратПоДокументу;
   Спр.СуммаТрудозатрат = Стр.СуммаТрудозатрат;
   Спр.СуммаМатериальныхЗатрат = Стр.СуммаМатериальныхЗатрат;
   
   Для каждого СтрТЧ1 из Стр.ТабЧасть1 цикл
	   СпрТЧ1 = Спр.ВидыРабот.Добавить();
	   СпрТЧ1.ВидРаботы = справочники.ВидыРабот.НайтиПоНаименованию(СтрТЧ1.ВидРаботы);
	   СпрТЧ1.Время = СтрТЧ1.Время;
	   СпрТЧ1.Цена = СтрТЧ1.Цена;
	   СпрТЧ1.Сумма = СтрТЧ1.Сумма;
   КонецЦикла;
   
   Для каждого СтрТЧ2 из Стр.ТабЧасть2 цикл
	   СпрТЧ2 = Спр.ЗатраченныеМатериалы.Добавить();
	   СпрТЧ2.Номенклатура = Справочники.Номенклатура.НайтиПоНаименованию(СтрТЧ2.Номенклатура1);
	   СпрТЧ2.ЕдиницаИзмерения = справочники.ЕдиницыИзмерения.НайтиПоНаименованию(СтрТЧ2.ЕдИзм1);
	   СпрТЧ2.Количество = СтрТЧ2.КолВО;
	   СпрТЧ2.Сумма = СтрТЧ2.Сумма1;
   КонецЦикла;
   Спр.Записать(РежимЗаписиДокумента.Проведение);	
	КонецПроцедуры
Процедура ЗагрузитьБрак(СтрокаБрака) Экспорт 

	Чтение = новый ЧтениеJSON;
	Чтение.УстановитьСтроку(СтрокаБрака);  
	Браки = ПрочитатьJSON(Чтение);
	Для каждого СтрокаМассива из Браки цикл
		СоздатьБрак(СтрокаМассива);
	КонецЦикла;                   
	
	

КонецПроцедуры
	
Процедура СоздатьБрак(Стр)
    Проверка = документы.БракНаПроизводстве.НайтиПоНомеру(Стр.Номер);
	
	Если Проверка.Пустая() тогда
		Спр = Документы.БракНаПроизводстве.СоздатьДокумент();
	иначе
		спр = Проверка.ПолучитьОбъект()
	КонецЕсли;
	
	Спр.Номер = стр.Номер;
	Спр.Дата = ПрочитатьДатуJSON(Стр.Дата,ФорматДатыJSON.ISO);
	Спр.Изделие = Справочники.Номенклатура.НайтиПоНаименованию(Стр.Изделие);
	Спр.Количество = Стр.Количество;
	Спр.Бригада = Справочники.Бригады.НайтиПоНаименованию(Стр.Бригада);
	Спр.ЭтапПроизводства = Документы.ВыполнениеЭтапаПроизводства.НайтиПоНомеру(Стр.ЭтапПроизводства);
	Спр.ПричинаБрака = Стр.ПричинаБрака;
	

КонецПроцедуры
	
фильтр///////////////////
&НаСервере
Процедура СтатусЭтапаПриИзмененииНаСервере()
	Если ИспользоватьФильтр тогда
		СтатусЭтапаПриИзмененииНаСервере();
	иначе	
		Список.Отбор.Элементы.Очистить();
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура СтатусЭтапаПриИзменении(Элемент)
	НовыйЭлементОтбора = Список.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ПолеОтбора = новый ПолеКомпоновкиДанных("Статус");
	НовыйЭлементОтбора.ЛевоеЗначение = ПолеОтбора;
	НовыйЭлементОтбора.ВидСравнения = ВидСравненияКомпоновкиДанных.Равно;
	НовыйЭлементОтбора.ПравоеЗначение = СтатусЭтапа;
КонецПроцедуры
/////////////////////////////////////////////////////////////
моб

процедура ОбновлениеНоменклатуры() экспорт
	Соединение = новый HTTPСоединение(константы.Сервер.Получить(),,,);
	Запрос = новый HTTPЗапрос(Константы.НаименованиеПубликации.Получить()+"/hs/ex/nomenklatury");
	
	Ответ = Соединение.Получить(Запрос);
	
	МассивНоменклатур =ПрочитатьJSONОтвет(Ответ);
	
	Для каждого Стр из МассивНоменклатур цикл
		Проверка = Справочники.Номенклатура.НайтиПоНаименованию(Стр.Наименование);
		
		Если Проверка.Пустая() тогда
			Спр = справочники.Номенклатура.СоздатьЭлемент();
		иначе
			спр = проверка.ПолучитьОбъект();
		КонецЕсли;
		
		Спр.Код = Стр.Код;
		Спр.Наименование = Стр.Наименование;
		Спр.Артикул = Стр.Артикул;
		ЕдИзм = справочники.ЕдиницыИзмерения.НайтиПоНаименованию(Стр.ЕдиницаИзмерения);
		Спр.ЕдиницаИзмерения = ЕдИзм;
		если Стр.Тип = "Сырье" тогда
			Спр.Тип = перечисления.ТипыНоменклатуры.Сырье;
		ИначеЕсли Стр.Тип = "Полуфабрикат" тогда
			Спр.Тип = перечисления.ТипыНоменклатуры.Полуфабрикат;
		ИначеЕсли Стр.Тип ="ГотоваяПродукция" тогда
			Спр.Тип = Перечисления.ТипыНоменклатуры.ГотоваяПродукция;
		КонецЕсли;
		Спецификация = справочники.Спецификации.НайтиПоНаименованию(Стр.Спецификация);
		Спр.Спецификация =Спецификация;
		Спр.Записать();
		
		
	КонецЦикла;
	
КонецПроцедуры

Процедура ОбновлениеЕдиницИзмерения() экспорт 
	Соединение = новый HTTPСоединение(константы.Сервер.Получить(),,,);
	Запрос = новый HTTPЗапрос(Константы.НаименованиеПубликации.Получить()+"/hs/ex/edizm");
	
	Ответ = Соединение.Получить(Запрос);
	
	МассивЕдиниц =ПрочитатьJSONОтвет(Ответ);
	
	Для каждого Стр из МассивЕдиниц цикл
		Проверка = Справочники.ЕдиницыИзмерения.НайтиПоНаименованию(Стр.Наименование);
		
		Если Проверка.Пустая() тогда
			Спр = справочники.ЕдиницыИзмерения.СоздатьЭлемент();
		иначе
			спр = проверка.ПолучитьОбъект();
		КонецЕсли;
		
		Спр.Код = Стр.Код;
		Спр.Наименование = Стр.Наименование;
		спр.Записать(); 
	КонецЦикла;
	
КонецПроцедуры

Процедура ОбновлениеБригад() экспорт 
	Соединение = новый HTTPСоединение(константы.Сервер.Получить(),,,);
	Запрос = новый HTTPЗапрос(Константы.НаименованиеПубликации.Получить()+"/hs/ex/brigady");
	
	Ответ = Соединение.Получить(Запрос);
	
	МассивБригад =ПрочитатьJSONОтвет(Ответ);
	
	Для каждого Стр из МассивБригад цикл
		Проверка = Справочники.Бригады.НайтиПоНаименованию(Стр.Наименование);
		
		Если Проверка.Пустая() тогда
			Спр = справочники.Бригады.СоздатьЭлемент();
		иначе
			спр = проверка.ПолучитьОбъект();
		КонецЕсли;
		
		Спр.Код = Стр.Код;
		Спр.Наименование = Стр.Наименование; 
		
		Для каждого Сотрудник из Стр.ТабЧасть цикл
			Спр.СоставБригады = Справочники.Сотрудники.НайтиПоНаименованию(Сотрудник.Сотрудник);
		КонецЦикла;
		
		Спр.Записать();
	КонецЦикла;
	
	
КонецПроцедуры 

Процедура ОбновлениеСотрудников() экспорт 
	Соединение = новый HTTPСоединение(константы.Сервер.Получить(),,,);
	Запрос = новый HTTPЗапрос(Константы.НаименованиеПубликации.Получить()+"/hs/ex/sotrudniki");
	
	Ответ = Соединение.Получить(Запрос);
	
	МассивСотрудников =ПрочитатьJSONОтвет(Ответ);
	
	Для каждого Стр из МассивСотрудников цикл
		Проверка = Справочники.Сотрудники.НайтиПоНаименованию(Стр.Наименование);
		
		Если Проверка.Пустая() тогда
			Спр = справочники.Сотрудники.СоздатьЭлемент();
		иначе
			спр = проверка.ПолучитьОбъект();
		КонецЕсли;
		
		Спр.Код = Стр.Код;
		Спр.Наименование = Стр.Наименование; 
		
		спр.Записать();
	КонецЦикла;
	
КонецПроцедуры

Процедура ОбновлениеСпецификаций() экспорт
	
	Соединение = новый HTTPСоединение(константы.Сервер.Получить(),,,);
	Запрос = новый HTTPЗапрос(Константы.НаименованиеПубликации.Получить()+"/hs/ex/spesifikacii");
	
	Ответ = Соединение.Получить(Запрос);
	
	МассивСпецификаций =ПрочитатьJSONОтвет(Ответ);
	
	Для каждого Стр из МассивСпецификаций цикл
		Проверка = Справочники.Спецификации.НайтиПоНаименованию(Стр.Наименование);
		
		Если Проверка.Пустая() тогда
			Спр = справочники.Спецификации.СоздатьЭлемент();
		иначе
			спр = проверка.ПолучитьОбъект();
		КонецЕсли;
		
		Спр.Код = Стр.Код;
		Спр.Наименование = Стр.Наименование; 
		Спр.ВыходноеИзделие = Справочники.Номенклатура.НайтиПоНаименованию(Стр.ВыходноеИзделие);
		спр.Записать();
	КонецЦикла;
	
	
	
КонецПроцедуры  

Процедура ОбновлениеСкладов() экспорт
	Соединение = новый HTTPСоединение(Константы.Сервер.Получить(),,,);
	Запрос = Новый HTTPЗапрос(Константы.НаименованиеПубликации.Получить()+"/hs/ex/sklady");
	
	Ответ = Соединение.Получить(Запрос);
	
	МассивСкладов = ПрочитатьJSONОтвет(Ответ);
	
	Для Каждого Стр из МассивСкладов цикл
		
		Проверка = справочники.Склады.НайтиПоНаименованию(Стр.Наименование);
		
		если проверка.Пустая() тогда
			Спр = справочники.Склады.СоздатьЭлемент();
		иначе
			Спр = проверка.ПолучитьОбъект();
		КонецЕсли;
		
		
		Спр.Наименование = Стр.Наименование;
		Спр.Код = Стр.Код;
		спр.Записать();
	КонецЦикла;
	
КонецПроцедуры

Процедура ОбновлениеВидовРабов() экспорт
	Соединение = новый HTTPСоединение(Константы.Сервер.Получить(),,,);
	Запрос = новый HTTPЗапрос(Константы.НаименованиеПубликации.Получить()+"/hs/ex/vidyrabot");
	
	Ответ = соединение.Получить(Запрос);
	
	МассивРабот = ПрочитатьJSONОтвет(Ответ);
	
	Для каждого Стр из МассивРабот цикл
		
		Проверка = справочники.ВидыРабот.НайтиПоНаименованию(Стр.Наименование);
		
		если проверка.Пустая() тогда
			спр = справочники.ВидыРабот.СоздатьЭлемент();
		иначе
			спр = проверка.ПолучитьОбъект();
		КонецЕсли;
		
		Спр.Наименование = Стр.Наименование;
		Спр.Код = Стр.Код;
		Спр.Записать();
	КонецЦикла;
	
	
КонецПроцедуры

процедура ОбновлениеЗаказов() экспорт
	Соединение = новый HTTPСоединение(Константы.Сервер.Получить(),,,);
	Запрос = новый HTTPЗапрос(Константы.НаименованиеПубликации.Получить()+"/hs/ex/zakazy");
	
	Ответ = соединение.Получить(Запрос);
	
	МассивЗаказов = ПрочитатьJSONОтвет(Ответ);
	
	Для каждого Стр из МассивЗаказов цикл
		проверка = документы.ЗаказыНаПроизводство.НайтиПоНомеру(Стр.Номер);
		
		Если проверка.Пустая() тогда
			Спр = документы.ЗаказыНаПроизводство.СоздатьДокумент();
		иначе
			Спр = проверка.ПолучитьОбъект();
		КонецЕсли;
		
		Спр.Дата = ПрочитатьДатуJSON(Стр.Дата,ФорматДатыJSON.ISO);
		Спр.Номер = Стр.Номер;
		СпрТЧ = спр.СписокИзделий.Добавить();
		Для каждого СтрТЧ из Стр.ТабЧасть цикл
			СпрТЧ.Изделие = Справочники.Номенклатура.НайтиПоНаименованию(СтрТЧ.Изделие);
			СпрТЧ.ЕдиницаИзмерения = Справочники.ЕдиницыИзмерения.НайтиПоНаименованию(СтрТЧ.ЕдИзм);
			СпрТЧ.Количество = СтрТЧ.Количество;
			СпрТЧ.Спецификация = справочники.Спецификации.НайтиПоНаименованию(СтрТЧ.Спецификация);
		КонецЦикла;
		
		спр.Записать(РежимЗаписиДокумента.Проведение);
	КонецЦикла;
КонецПроцедуры 

процедура ОбновлениеЭтапов() экспорт
Соединение = новый HTTPСоединение(Константы.Сервер.Получить(),,,);
Запрос = новый HTTPЗапрос(константы.НаименованиеПубликации.Получить()+"/hs/ex/etapi");

Ответ = Соединение.Получить(Запрос);

МассивЭтапов = ПрочитатьJSONОтвет(Ответ);

Для каждого Стр из МассивЭтапов цикл
	Проверка = документы.ВыполнениеЭтапаПроизводства.НайтиПоНомеру(Стр.Номер);
	
	Если проверка.Пустая() тогда
		спр = документы.ВыполнениеЭтапаПроизводства.СоздатьДокумент();
	иначе
		спр = проверка.ПолучитьОбъект();
	КонецЕсли;
	
	   Спр.Номер = Стр.Номер;
	   Спр.Дата = ПрочитатьДатуJSON(Стр.Дата,ФорматДатыJSON);
	   Спр.Бригада = справочники.Бригады.НайтиПоНаименованию(Стр.Бригада);
	   Спр.Проведен = Стр.Проведен;
	   Спр.ЗаказНаПроизводство = Документы.ЗаказыНаПроизводство.НайтиПоНомеру(Стр.Заказ);
	   Спр.Изделие = Справочники.Номенклатура.НайтиПоНаименованию(Стр.Изделие);
	   Спр.Количество = Стр.Количество;
	   Спр.ЕдиницаИзмерения = Справочники.ЕдиницыИзмерения.НайтиПоНаименованию(Стр.ЕдИзм); 
	   Если Стр.Статус = "В работе" тогда
	   Спр.Статус = перечисления.СтатусыЭтаповПроизводства.ВРаботе;
   ИначеЕсли Стр.Статус = "Завершен" тогда
	   Спр.статус = перечисления.СтатусыЭтаповПроизводства.Завершен;
   ИначеЕсли Стр.Статус = "Запланирован" тогда
	   Спр.Статус = перечисления.СтатусыЭтаповПроизводства.Запланирован
   КонецЕсли; 
   Спр.СкладМатериалов = справочники.Склады.НайтиПоНаименованию(Стр.СкладМ);
   Спр.СкладГотовойПродукции = справочники.Склады.НайтиПоНаименованию(Стр.СкладГ);
   Спр.СуммаЗатратПоДокументу = Стр.СуммаЗатратПоДокументу;
   Спр.СуммаТрудозатрат = Стр.СуммаТрудозатрат;
   Спр.СуммаМатериальныхЗатрат = Стр.СуммаМатериальныхЗатрат;
   
   Для каждого СтрТЧ1 из Стр.ТабЧасть1 цикл
	   СпрТЧ1 = Спр.ВидыРабот.Добавить();
	   СпрТЧ1.ВидРаботы = справочники.ВидыРабот.НайтиПоНаименованию(СтрТЧ1.ВидРаботы);
	   СпрТЧ1.Время = СтрТЧ1.Время;
	   СпрТЧ1.Цена = СтрТЧ1.Цена;
	   СпрТЧ1.Сумма = СтрТЧ1.Сумма;
   КонецЦикла;
   
   Для каждого СтрТЧ2 из Стр.ТабЧасть2 цикл
	   СпрТЧ2 = Спр.ЗатраченныеМатериалы.Добавить();
	   СпрТЧ2.Номенклатура = Справочники.Номенклатура.НайтиПоНаименованию(СтрТЧ2.Номенклатура1);
	   СпрТЧ2.ЕдиницаИзмерения = справочники.ЕдиницыИзмерения.НайтиПоНаименованию(СтрТЧ2.ЕдИзм1);
	   СпрТЧ2.Количество = СтрТЧ2.КолВО;
	   СпрТЧ2.Сумма = СтрТЧ2.Сумма1;
   КонецЦикла;
   Спр.Записать(РежимЗаписиДокумента.Проведение);
КонецЦикла;

	

КонецПроцедуры

процедура ОбновлениеПеремещений() экспорт

	Соединение = новый HTTPСоединение(Константы.Сервер.Получить(),,,);
	Запрос = новый HTTPЗапрос(Константы.НаименованиеПубликации.Получить()+"/hs/ex/peremeshenia");
	
	Ответ = Соединение.Получить(Запрос);
	
	МассивПеремещений = ПрочитатьJSONОтвет(Ответ);
	
	Для каждого Стр из МассивПеремещений цикл
	Проверка = документы.ПеремещениеМатериалов.НайтиПоНомеру(Стр.Номер);
	
	если проверка.Пустая() тогда
		Спр = документы.ПеремещениеМатериалов.СоздатьДокумент();
	иначе
		спр = проверка.ПолучитьОбъект();
	КонецЕсли;
	
	Спр.Номер = Стр.Номер;
	Спр.Дата = ПрочитатьДатуJSON(Стр.Дата,ФорматДатыJSON.ISO);
	Спр.Проведен = Стр.Проведен;
	Спр.СкладОтправитель = справочники.Склады.НайтиПоНаименованию(Стр.СкладО);
	Спр.СкладПолучатель = справочники.Склады.НайтиПоНаименованию(Стр.СкладП);
	
	Для каждого СтрТЧ из Стр.ТабЧасть1 цикл
		СпрТЧ = спр.ТабличнаяЧасть1.Добавить();
		СпрТЧ.Номенклатура = Справочники.Номенклатура.НайтиПоНаименованию(СтрТЧ.НоменклатураПредставление);
		СпрТЧ.Количество = СтрТЧ.Количество;
		
	КонецЦикла;
	    спр.Записать(РежимЗаписиДокумента.Проведение);
	КонецЦикла;
КонецПроцедуры

процедура ОбновлениеРеализаций() экспорт
Соединение = новый HTTPСоединение(Константы.Сервер.Получить(),,,);
запрос = новый HTTPЗапрос(Константы.НаименованиеПубликации.Получить()+"/hs/ex/realizacia");

Ответ = Соединение.Получить(запрос);

МассивРеализаций = ПрочитатьJSONОтвет(Ответ);

Для каждого Стр из МассивРеализаций цикл
Проверка = документы.РеализацияГотовойПродукции.НайтиПоНомеру(Стр.Номер);

если проверка.Пустая() тогда
	Спр = документы.РеализацияГотовойПродукции.СоздатьДокумент();
иначе
	спр = проверка.ПолучитьОбъект();
КонецЕсли;

Спр.Номер = стр.номер;
спр.Дата = ПрочитатьДатуJSON(Стр.Дата,ФорматДатыJSON.ISO);


КонецЦикла;

КонецПроцедуры


Функция ПрочитатьJSONОтвет(Данные) 
	Чтение = новый ЧтениеJSON;
	Чтение.УстановитьСтроку(Данные.ПолучитьТелоКакСтроку());
	Массив =ПрочитатьJSON(Чтение);
	
	Возврат Массив;
КонецФункции



&НаКлиенте
Процедура ОтправитьДанныеОПеремещениях(Команда)
	 Сообщение = Новый СообщениеПользователю;

	Сервер = "localhost";
	Попытка
		Соединение = новый HTTPСоединение(Сервер,,,,);
	Исключение
		Сообщение.Текст = ОписаниеОшибки();
		Сообщение.Сообщить();
		Возврат;
	КонецПопытки;
	
	АдресРесурса ="";
	Заголовки = новый Соответствие;
	Заголовки.Вставить("Content-type","application/json");
	
	Запрос = новый HTTPЗапрос(АдресРесурса,Заголовки);
	
	СтрокаЗадач = ПолучитьСтрокуЗапроса();
	
	Запрос.УстановитьТелоИзСтроки(СтрокаЗадач);
	
	Попытка
		Соединение.ОтправитьДляОбработки(Запрос);
	Исключение
      		Сообщение.Текст = ОписаниеОшибки();
		Сообщение.Сообщить();           
	КонецПопытки;
	
	КонецПроцедуры
	
	Функция ПолучитьСтрокуЗапроса()
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	ПеремещениеМатериалов.Дата КАК Дата,
		|	ПеремещениеМатериалов.Номер КАК Номер,
		|	ПеремещениеМатериалов.СкладОтправитель.Наименование КАК СкладОтправительНаименование,
		|	ПеремещениеМатериалов.СкладПолучатель.Наименование КАК СкладПолучательНаименование,
		|	ПеремещениеМатериаловТабличнаяЧасть1.Номенклатура.Представление КАК НоменклатураПредставление,
		|	ПеремещениеМатериаловТабличнаяЧасть1.Количество КАК Количество
		|ИЗ
		|	Документ.ПеремещениеМатериалов.ТабличнаяЧасть1 КАК ПеремещениеМатериаловТабличнаяЧасть1
		|		ЛЕВОЕ СОЕДИНЕНИЕ Документ.ПеремещениеМатериалов КАК ПеремещениеМатериалов
		|		ПО ПеремещениеМатериаловТабличнаяЧасть1.Ссылка = ПеремещениеМатериалов.Ссылка";
	
	РезультатЗапроса = Запрос.Выполнить();
	
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	НомерДокумента = 0;
	Массив = новый массив;
	МассивТЧ = Неопределено;
	Пока ВыборкаДетальныеЗаписи.Следующий() Цикл
		
		Если ВыборкаДетальныеЗаписи.Номер <> НомерДокумента тогда
			
			Стр = новый Структура;
			Если МассивТЧ <> Неопределено тогда
				Стр.Вставить("Табчасть",МассивТЧ);
				Массив.Добавить(Стр);
			КонецЕсли;
			
			Стр.Вставить("Номер",ВыборкаДетальныеЗаписи.Номер);
			Стр.Вставить("Дата",ВыборкаДетальныеЗаписи.Дата);
			Стр.Вставить("СкладОтправитель",ВыборкаДетальныеЗаписи.СкладОтправительНаименование);
			Стр.Вставить("СкладПолучатель",ВыборкаДетальныеЗаписи.СкладПолучательНаименование);
			
			МассивТЧ = новый массив;
			СтрТЧ = новый Структура;
			СтрТЧ.Вставить("Номенклатура",ВыборкаДетальныеЗаписи.НоменклатураПредставление);
			СтрТЧ.Вставить("Количество",ВыборкаДетальныеЗаписи.Количество);
			МассивТЧ.Добавить(СтрТЧ);
			Стр.Вставить("ТабЧасть",МассивТЧ);
			Массив.Добавить(Стр);
	КонецЦикла;
	  Запись = новый ЗаписьJSON;
	  Запись.УстановитьСтроку();
	  ЗаписатьJSON(Запись,Массив);
	  РезультатJSON = запись.Закрыть();
	  
	  возврат РезультатJSON;
	
	КонецФункции 
	


&НаКлиенте
Процедура ОтправитьДанныеОВыполненныхЭтапах(Команда)
	Сервер = "localhost";
	Попытка
		Соединение = новый HTTPСоединение(Сервер,,,);
	Исключение
		Сообщение.Текст = ОписаниеОшибки();
		Сообщение.Сообщить();
	КонецПопытки;
	
	АдресРесурса = "";
	Заголовки = новый Соответствие;
	Заголовки.Вставить("Content-type","application/json");
	
	Запрос = новый HTTPЗапрос(АдресРесурса,Заголовки);
	
	СтрокаЭтапов =ПолучитьСтрокуЗапроса();
	Запрос.УстановитьТелоИзСтроки(СтрокаЭтапов);
	
	Попытка
		Соединение.ОтправитьДляОбработки(Запрос);
	исключение
		Сообщение.Текст = ОписаниеОшибки();
		Сообщение.Сообщить();
	КонецПопытки;
	
КонецПроцедуры

Функция ПолучитьСтрокуЗапроса()
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	ВыполнениеЭтапаПроизводства.Номер КАК Номер,
	|	ВыполнениеЭтапаПроизводства.Дата КАК Дата,
	|	ВыполнениеЭтапаПроизводства.ЗаказНаПроизводство.Номер КАК ЗаказНаПроизводствоНомер,
	|	ВыполнениеЭтапаПроизводства.Изделие.Наименование КАК ИзделиеНаименование,
	|	ВыполнениеЭтапаПроизводства.Количество КАК Количество,
	|	ВыполнениеЭтапаПроизводства.ЕдиницаИзмерения.Наименование КАК ЕдиницаИзмеренияНаименование,
	|	ВыполнениеЭтапаПроизводства.Бригада.Наименование КАК БригадаНаименование,
	|	ВыполнениеЭтапаПроизводства.СкладГотовойПродукции.Наименование КАК СкладГотовойПродукцииНаименование,
	|	ПРЕДСТАВЛЕНИЕ(ВыполнениеЭтапаПроизводства.Статус) КАК СтатусПредставление,
	|	ВыполнениеЭтапаПроизводства.СкладМатериалов.Наименование КАК СкладМатериаловНаименование,
	|	ВыполнениеЭтапаПроизводства.СуммаЗатратПоДокументу КАК СуммаЗатратПоДокументу,
	|	ВыполнениеЭтапаПроизводства.СуммаТрудозатрат КАК СуммаТрудозатрат,
	|	ВыполнениеЭтапаПроизводства.СуммаМатериальныхЗатрат КАК СуммаМатериальныхЗатрат,
	|	ВыполнениеЭтапаПроизводстваВидыРабот.ВидРаботы.Наименование КАК ВидРаботыНаименование,
	|	ВыполнениеЭтапаПроизводстваВидыРабот.Цена КАК Цена,
	|	ВыполнениеЭтапаПроизводстваВидыРабот.Время КАК Время,
	|	ВыполнениеЭтапаПроизводстваВидыРабот.Сумма КАК Сумма,
	|	ВыполнениеЭтапаПроизводстваЗатраченныеМатериалы.Номенклатура.Наименование КАК НоменклатураНаименование,
	|	ВыполнениеЭтапаПроизводстваЗатраченныеМатериалы.Количество КАК Количество1,
	|	ВыполнениеЭтапаПроизводстваЗатраченныеМатериалы.Цена КАК Цена1,
	|	ВыполнениеЭтапаПроизводстваЗатраченныеМатериалы.Сумма КАК Сумма1,
	|	ВыполнениеЭтапаПроизводстваЗатраченныеМатериалы.ЕдиницаИзмерения.Наименование КАК ЕдиницаИзмеренияНаименование1
	|ИЗ
	|	Документ.ВыполнениеЭтапаПроизводства.ЗатраченныеМатериалы КАК ВыполнениеЭтапаПроизводстваЗатраченныеМатериалы
	|		ЛЕВОЕ СОЕДИНЕНИЕ Документ.ВыполнениеЭтапаПроизводства КАК ВыполнениеЭтапаПроизводства
	|			ПРАВОЕ СОЕДИНЕНИЕ Документ.ВыполнениеЭтапаПроизводства.ВидыРабот КАК ВыполнениеЭтапаПроизводстваВидыРабот
	|			ПО (ВыполнениеЭтапаПроизводстваВидыРабот.Ссылка = ВыполнениеЭтапаПроизводства.Ссылка)
	|		ПО ВыполнениеЭтапаПроизводстваЗатраченныеМатериалы.Ссылка = ВыполнениеЭтапаПроизводства.Ссылка";
	
	РезультатЗапроса = Запрос.Выполнить();
	
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	Массив = новый Массив;
	МассивТЧ1 = Неопределено;
	МассивТЧ2 = Неопределено;
	НомерДокумента = 0;
	Пока ВыборкаДетальныеЗаписи.Следующий() Цикл
		если ВыборкаДетальныеЗаписи.Номер <> НомерДокумента тогда
			НомерДокумента = ВыборкаДетальныеЗаписи.Номер;
			Стр = новый Структура;
			Если МассивТЧ1 <> Неопределено тогда 
				Стр.Вставить("ТабЧасть1",МассивТЧ1);
				Массив.Добавить(Стр);
			КонецЕсли;
			Если МассивТЧ2 <> Неопределено тогда 
				Стр.Вставить("ТабЧасть2",МассивТЧ2);
				Массив.Добавить(Стр);
			КонецЕсли;
			Стр.Вставить("Номер",ВыборкаДетальныеЗаписи.Номер);
			Стр.Вставить("Дата",ВыборкаДетальныеЗаписи.Дата);
			Стр.Вставить("Проведен",ВыборкаДетальныеЗаписи.Проведен);
			Стр.Вставить("Заказ",ВыборкаДетальныеЗаписи.ЗаказНаПроизводствоНомер);
			Стр.Вставить("Изделие",ВыборкаДетальныеЗаписи.ИзделиеНаименование);
			Стр.Вставить("Количество",ВыборкаДетальныеЗаписи.Количество);
			Стр.Вставить("ЕдИзм",ВыборкаДетальныеЗаписи.ЕдиницаИзмеренияНаименование);
			Стр.Вставить("Бригада",ВыборкаДетальныеЗаписи.БригадаНаименование);
			Стр.Вставить("СкладГ",ВыборкаДетальныеЗаписи.СкладГотовойПродукцииНаименование);
			Стр.Вставить("Статус",ВыборкаДетальныеЗаписи.СтатусПредставление);
			Стр.Вставить("СкладМ",ВыборкаДетальныеЗаписи.СкладМатериаловНаименование);
			Стр.Вставить("СуммаЗатратПоДокументу",ВыборкаДетальныеЗаписи.СуммаЗатратПоДокументу);
			Стр.Вставить("СуммаТрудозатрат",ВыборкаДетальныеЗаписи.СуммаТрудозатрат);
			Стр.Вставить("СуммаМатериальныхЗатрат",ВыборкаДетальныеЗаписи.СуммаМатериальныхЗатрат);
			
			МассивТЧ1 = новый Массив;
			СтрТЧ1 = новый Структура;
			СтрТЧ1.Вставить("ВидРаботы",ВыборкаДетальныеЗаписи.ВидРаботыНаименование);
			СтрТЧ1.Вставить("Цена",ВыборкаДетальныеЗаписи.Цена);
			СтрТЧ1.Вставить("Время",ВыборкаДетальныеЗаписи.Время);
			СтрТЧ1.Вставить("Сумма",ВыборкаДетальныеЗаписи.Сумма);
			МассивТЧ1.Добавить(СтрТЧ1);
			
			МассивТЧ2 = новый Массив;
			СтрТЧ2 = новый Структура;
			СтрТЧ2.Вставить("Номенклатура1",ВыборкаДетальныеЗаписи.НоменклатураНаименование);
			СтрТЧ2.Вставить("Цена1",ВыборкаДетальныеЗаписи.Цена1);
			СтрТЧ2.Вставить("Сумма1",ВыборкаДетальныеЗаписи.Сумма1);
			СтрТЧ2.Вставить("ЕдИзм1",ВыборкаДетальныеЗаписи.ЕдиницаИзмеренияНаименование1);
			СтрТЧ2.Вставить("КолВО",ВыборкаДетальныеЗаписи.Количество1);
			МассивТЧ2.Добавить(СтрТЧ2);
			
			Стр.Вставить("ТабЧасть1",МассивТЧ1);
			Стр.Вставить("ТабЧасть2",МассивТЧ2);
			Массив.Добавить(Стр);
			
			
		КонецЕсли;
		
	КонецЦикла;
	Запись = новый ЗаписьJSON;
	Запись.УстановитьСтроку();
	ЗаписатьJSON(Запись,Массив);
	РезультатJSON = запись.Закрыть();
	
	возврат РезультатJSON;
	
	
	
КонецФункции // ПолучитьСтрокуЗапроса()
